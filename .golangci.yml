run:
  deadline: 30m
  tests: true

linters-settings:
  govet:
    check-shadowing: true
  gofmt:
    simplify: true
  maligned:
    suggest-new: true

linters:
  enable:
    - gofmt
    - misspell
    - maligned
  disable:
    - goimports
    - errcheck
